import 'package:flutter/material.dart';

class MySammelkarte extends StatelessWidget {
  const MySammelkarte({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
      height: 200,
      margin: const EdgeInsets.all(15.0),
      padding: EdgeInsets.all(3.0),
      width: 140,
      color: Colors.red,
    );
  }
}